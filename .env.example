# Copy this file, rename it to .env and fill in the values
# .env should be gitignored — never commit real credentials
GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_KEY_HERE
DB_USERNAME=salon_user
DB_PASSWORD=salon_pass
DB_HOST=localhost
DB_PORT=0000
DB_NAME=example-db